# Project place file
/example.rbxlx

# Roblox Studio lock files
/*.rbxlx.lock
/*.rbxl.lock
/*.rbxl

# Generated
/sourcemap.json

# Wally packages — restore with `wally install` (versions pinned in wally.lock)
/Packages/

# Local AI/dev handoff notes (machine-specific paths)
/CLAUDE.md

# Imported UI-framework REFERENCE docs — dev-only (the framework CODE the game uses lives in src/std/).
# Kept locally for the Studio/dev copy; not part of the public portfolio repo.
/docs/framework/

# Imported VEIN framework — the game only uses Data/ (the ProfileStore wrapper), which IS tracked.
# The rest (Session/Registry/Rodux/UI/Packets/…) is local reference, not part of this build.
/src/Framework/*
!/src/Framework/Data/

# Claude Code tooling — skills, agents, local settings. Dev-only.
/.claude/
