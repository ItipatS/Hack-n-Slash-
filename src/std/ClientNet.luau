--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.18.7 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
Queue.Unreliable[0] = table.create(256)
Queue.Reliable[1] = table.create(256)
Events.Reliable[2] = {}
Queue.Reliable[2] = table.create(256)
Queue.Reliable[3] = table.create(256)
Queue.Reliable[4] = table.create(256)
Queue.Reliable[5] = table.create(256)
Queue.Reliable[6] = table.create(256)
Events.Reliable[7] = {}
Queue.Reliable[7] = table.create(256)
Events.Reliable[8] = {}
Queue.Reliable[8] = table.create(256)
Queue.Reliable[9] = table.create(256)
Queue.Unreliable[1] = table.create(256)
Queue.Reliable[10] = table.create(256)
Queue.Reliable[11] = table.create(256)
Queue.Reliable[12] = table.create(256)
Queue.Reliable[15] = table.create(256)
Events.Reliable[18] = {}
Queue.Reliable[18] = table.create(256)
Events.Reliable[19] = {}
Queue.Reliable[19] = table.create(256)
Queue.Reliable[20] = table.create(256)
Events.Reliable[24] = {}
Queue.Reliable[24] = table.create(256)
Events.Reliable[25] = {}
Queue.Reliable[25] = table.create(256)
Queue.Reliable[26] = table.create(256)
Queue.Reliable[27] = table.create(256)
Queue.Reliable[28] = table.create(256)
Queue.Reliable[29] = table.create(256)
Queue.Reliable[30] = table.create(256)
Queue.Reliable[31] = table.create(256)
Queue.Reliable[32] = table.create(256)
Events.Reliable[33] = {}
Queue.Reliable[33] = table.create(256)
Events.Unreliable[3] = {}
Queue.Unreliable[3] = table.create(256)
Queue.Reliable[34] = table.create(256)
Queue.Reliable[35] = table.create(256)
Queue.Reliable[36] = table.create(256)
Queue.Reliable[37] = table.create(256)
Queue.Reliable[38] = table.create(256)
Queue.Reliable[39] = table.create(256)
Queue.Reliable[40] = table.create(256)
Queue.Reliable[41] = table.create(256)
Queue.Reliable[42] = table.create(256)
Events.Reliable[43] = {}
Queue.Reliable[43] = table.create(256)
Queue.Reliable[44] = table.create(256)
Queue.Unreliable[4] = table.create(256)
Queue.Reliable[45] = table.create(256)
Queue.Unreliable[5] = table.create(256)
export type state = {[string]: string}
export type DeltaChunk = { base: number, deltas: { number } }
export type TransformFull = { id: number, dp: Vector3 }
export type MobState = { id: number, state: {[string]: string} }
export type MobSpawn = { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number }
export type Path = { id: number, from: number, to: number, speed: number, startTime: number }
export type Chase = { id: number, target: Instance, speed: number }
export type BubbleFire = { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? }
export type ExpSpawn = { id: number, pos: Vector3, tier: number }
export type Card = { id: number, title: string, desc: string, kind: number, weapon: string }
export type StatsMsg = { level: number, xp: number, xpNext: number, hp: number, maxHp: number }
export type StatSheetMsg = { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }
export type GameOverMsg = { time: number, level: number }
export type MobHp = { id: number, hp: number, max: number }
export type Offer = { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }
export type WeaponInfo = { id: string, level: number }
export type ItemMsg = { id: string, level: number }
export type EffectMsg = { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? }
export type HazardMsg = { id: number, kind: number, pos: Vector3, radius: number, life: number }
export type SummonMsg = { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number }
export type BeamState = { active: number, life: number, width: number, length: number }
export type PetDrop = { id: number, pos: Vector3, pet: string }
export type CardDrop = { id: number, pos: Vector3 }
export type HealthDrop = { id: number, pos: Vector3 }
export type TixDrop = { id: number, pos: Vector3, amount: number }
export type ProfileMsg = { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }
export type UfoPos = { id: number, pos: Vector3 }
export type UfoBolt = { origin: Vector3, vel: Vector3, t0: number, life: number }
export type UfoBeamMsg = { id: number, target: Vector3, charging: number }
function SerdesFunctions.WriteEVENT_PlayerAction(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.ReadEVENT_UpdateTransformDelta(): ({ base: number, deltas: { number } })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Value = {} :: any
	Value.base = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	Value.deltas = table.create(Length)
		-- Read BLOCK: 1 bytes
	local ARRAY_START_1 = Read(1 * Length)
	for Index = 1, Length do
		-- Read 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		local Item_1 = buffer.readi8(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value.deltas, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UpdateTransformFull(): ({ { id: number, dp: Vector3 } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 16 bytes
	local ARRAY_START_1 = Read(16 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.dp = Vector3.new(X, Y, Z)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_Spawn(): ({ { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 37 bytes
	local ARRAY_START_1 = Read(37 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.slot = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 8
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 8
		local X = buffer.readf64(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 8
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 8
		local Y = buffer.readf64(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 8
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 8
		local Z = buffer.readf64(RecieveBuffer, OPERATION_OFFSET_4)
		Item_1.pos = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.size = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
		-- Read 2
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_6)
		Item_1.type = buffer.readstring(RecieveBuffer, Read(Length), Length)
		-- Read 1
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.tier = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_7)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UpdateMobState(): ({ { id: number, state: {[string]: string} } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 6 bytes
	local ARRAY_START_1 = Read(6 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		Item_1.state = {}
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		local Elements_2 = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_1)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(2)
			Length = buffer.readu16(RecieveBuffer, OFFSET_0)
			local Key_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			local OFFSET_1 = Read(2)
			Length = buffer.readu16(RecieveBuffer, OFFSET_1)
			local Element_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			Item_1.state[Key_2] = Element_2
		end
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UpdateMobPath(): ({ { id: number, from: number, to: number, speed: number, startTime: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 24 bytes
	local ARRAY_START_1 = Read(24 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.from = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.to = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.speed = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 8
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 8
		Item_1.startTime = buffer.readf64(RecieveBuffer, OPERATION_OFFSET_4)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UpdateMobChase(): ({ { id: number, target: Instance, speed: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 8 bytes
	local ARRAY_START_1 = Read(8 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		RecieveInstanceCursor += 1
		Item_1.target = RecieveInstances[RecieveInstanceCursor]
		if typeof(Item_1.target) ~= "Instance" then error(`Expected an Instance, got {typeof(Item_1.target)} instead.`) end
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.speed = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_StatSheet(): ({ maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number })
	-- Read BLOCK: 50 bytes
	local BLOCK_START = Read(50)
	local Value = {} :: any
	Value.maxHp = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value.hpRegen = buffer.readf32(RecieveBuffer, BLOCK_START + 2)
	Value.atk = buffer.readf32(RecieveBuffer, BLOCK_START + 6)
	Value.walkSpeed = buffer.readf32(RecieveBuffer, BLOCK_START + 10)
	Value.crit = buffer.readf32(RecieveBuffer, BLOCK_START + 14)
	Value.critDmg = buffer.readf32(RecieveBuffer, BLOCK_START + 18)
	Value.pickup = buffer.readf32(RecieveBuffer, BLOCK_START + 22)
	Value.haste = buffer.readf32(RecieveBuffer, BLOCK_START + 26)
	Value.expMult = buffer.readf32(RecieveBuffer, BLOCK_START + 30)
	Value.tixRate = buffer.readf32(RecieveBuffer, BLOCK_START + 34)
	Value.atkSize = buffer.readf32(RecieveBuffer, BLOCK_START + 38)
	Value.knock = buffer.readf32(RecieveBuffer, BLOCK_START + 42)
	Value.skillCd = buffer.readf32(RecieveBuffer, BLOCK_START + 46)
	return Value
end

function SerdesFunctions.ReadEVENT_MobHealth(): ({ { id: number, hp: number, max: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 8 bytes
	local ARRAY_START_1 = Read(8 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.hp = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		Item_1.max = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_2)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_Death(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_FireBubble(): ({ { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 57 bytes
	local ARRAY_START_1 = Read(57 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.origin = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
		-- Read 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
		-- Read 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_6)
		Item_1.velocity = Vector3.new(X, Y, Z)
		-- Read 8
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 8
		Item_1.t0 = buffer.readf64(RecieveBuffer, OPERATION_OFFSET_7)
		-- Read 4
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.life = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_8)
		-- Read 4
		local OPERATION_OFFSET_9 = ARRAY_START_1
		ARRAY_START_1 += 4
		X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_9)
		-- Read 4
		local OPERATION_OFFSET_10 = ARRAY_START_1
		ARRAY_START_1 += 4
		Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_10)
		-- Read 4
		local OPERATION_OFFSET_11 = ARRAY_START_1
		ARRAY_START_1 += 4
		Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_11)
		Item_1.color = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_12 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.size = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_12)
		-- Read 1
		local OPERATION_OFFSET_13 = ARRAY_START_1
		ARRAY_START_1 += 1
		if buffer.readu8(RecieveBuffer, OPERATION_OFFSET_13) == 1 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			Item_1.shape = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		end
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_RemoveBubble(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_SpawnExp(): ({ { id: number, pos: Vector3, tier: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 17 bytes
	local ARRAY_START_1 = Read(17 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.pos = Vector3.new(X, Y, Z)
		-- Read 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.tier = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_4)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_RemoveExp(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_LevelUp(): ({ source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } })
	-- Read BLOCK: 3 bytes
	local BLOCK_START = Read(3)
	local Value = {} :: any
	Value.source = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 1)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	Value.cards = table.create(Length)
		-- Read BLOCK: 8 bytes
	local ARRAY_START_1 = Read(8 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.id = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_1)
		Item_1.title = buffer.readstring(RecieveBuffer, Read(Length), Length)
		-- Read 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_2)
		Item_1.desc = buffer.readstring(RecieveBuffer, Read(Length), Length)
		-- Read 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.kind = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 2
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_4)
		Item_1.weapon = buffer.readstring(RecieveBuffer, Read(Length), Length)
		table.insert(Value.cards, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_ChooseCard(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 13)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_Aim(Value: Vector3): ()
	-- Allocate BLOCK: 13 bytes
	local BLOCK_START = Allocate(13)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	local Vector = Value
	buffer.writef32(SendBuffer, BLOCK_START + 1, Vector.X)
	buffer.writef32(SendBuffer, BLOCK_START + 5, Vector.Y)
	buffer.writef32(SendBuffer, BLOCK_START + 9, Vector.Z)
end

function SerdesFunctions.WriteEVENT_UseSkill(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 14)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.ReadEVENT_GameOver(): ({ time: number, level: number })
	-- Read BLOCK: 6 bytes
	local BLOCK_START = Read(6)
	local Value = {} :: any
	Value.time = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
	Value.level = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
	return Value
end

function SerdesFunctions.WriteEVENT_Restart(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 16)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_SwitchPlace(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 17)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.ReadEVENT_RoundOver(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_Weapons(): ({ { id: string, level: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 3 bytes
	local ARRAY_START_1 = Read(3 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		Item_1.id = buffer.readstring(RecieveBuffer, Read(Length), Length)
		-- Read 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.level = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_Unlocked(): ({ string })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 2 bytes
	local ARRAY_START_1 = Read(2 * Length)
	for Index = 1, Length do
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		local Item_1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_SwitchWeapons(Value: { string }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 21)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 2 bytes
	local ARRAY_START_1 = Allocate(2 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		local Length = #Item_1
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1, Length)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_SetMain(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 22)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.WriteEVENT_BuyPet(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 23)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadEVENT_Items(): ({ { id: string, level: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 3 bytes
	local ARRAY_START_1 = Read(3 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		Item_1.id = buffer.readstring(RecieveBuffer, Read(Length), Length)
		-- Read 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.level = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_MainWeapon(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_Effect(): ({ { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 31 bytes
	local ARRAY_START_1 = Read(31 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.kind = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.pos = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.radius = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
		-- Read 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
		-- Read 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_6)
		-- Read 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_7)
		Item_1.dir = Vector3.new(X, Y, Z)
		-- Read 1
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 1
		if buffer.readu8(RecieveBuffer, OPERATION_OFFSET_8) == 1 then
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			local Encoded = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			local MantissaExponent = Encoded % 0x8000
			if MantissaExponent == 0b0_11111_0000000000  then
				if Encoded // 0x8000 == 1 then
					Item_1.arc = -math.huge
				else
					Item_1.arc = math.huge
				end
			elseif MantissaExponent == 0b1_11111_0000000000   then
				Item_1.arc = 0 / 0
			elseif MantissaExponent == 0b0_00000_0000000000   then
				Item_1.arc = 0
			else
				local Mantissa = MantissaExponent % 0x400
				local Exponent = MantissaExponent // 0x400
				local Fraction;
				if Exponent == 0 then
					Fraction = Mantissa / 0x400
				else
					Fraction = Mantissa / 0x800 + 0.5
				end
				local Result = math.ldexp(Fraction, Exponent - 14)
				Item_1.arc = if Encoded // 0x8000 == 1 then -Result else Result
			end
		end
		-- Read 1
		local OPERATION_OFFSET_9 = ARRAY_START_1
		ARRAY_START_1 += 1
		if buffer.readu8(RecieveBuffer, OPERATION_OFFSET_9) == 1 then
			-- Read BLOCK: 12 bytes
			local BLOCK_START = Read(12)
			X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
			Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
			Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
			Item_1.color = Vector3.new(X, Y, Z)
		end
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_SummonSpawn(): ({ { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 41 bytes
	local ARRAY_START_1 = Read(41 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.kind = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
		Item_1.origin = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
		-- Read 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_6)
		-- Read 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_7)
		Item_1.velocity = Vector3.new(X, Y, Z)
		-- Read 8
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 8
		Item_1.t0 = buffer.readf64(RecieveBuffer, OPERATION_OFFSET_8)
		-- Read 4
		local OPERATION_OFFSET_9 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.life = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_9)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_SummonRemove(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_HazardSpawn(): ({ { id: number, kind: number, pos: Vector3, radius: number, life: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 25 bytes
	local ARRAY_START_1 = Read(25 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.kind = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
		Item_1.pos = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.radius = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
		-- Read 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.life = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_6)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_HazardRemove(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UltBeam(): ({ active: number, life: number, width: number, length: number })
	-- Read BLOCK: 13 bytes
	local BLOCK_START = Read(13)
	local Value = {} :: any
	Value.active = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	Value.life = buffer.readf32(RecieveBuffer, BLOCK_START + 1)
	Value.width = buffer.readf32(RecieveBuffer, BLOCK_START + 5)
	Value.length = buffer.readf32(RecieveBuffer, BLOCK_START + 9)
	return Value
end

function SerdesFunctions.ReadEVENT_WaveStart(): (number)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Value = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_Stats(): ({ level: number, xp: number, xpNext: number, hp: number, maxHp: number })
	-- Read BLOCK: 14 bytes
	local BLOCK_START = Read(14)
	local Value = {} :: any
	Value.level = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value.xp = buffer.readu32(RecieveBuffer, BLOCK_START + 2)
	Value.xpNext = buffer.readu32(RecieveBuffer, BLOCK_START + 6)
	Value.hp = buffer.readu16(RecieveBuffer, BLOCK_START + 10)
	Value.maxHp = buffer.readu16(RecieveBuffer, BLOCK_START + 12)
	return Value
end

function SerdesFunctions.ReadEVENT_Hit(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_Toast(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

function SerdesFunctions.ReadEVENT_PetDropSpawn(): ({ { id: number, pos: Vector3, pet: string } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 18 bytes
	local ARRAY_START_1 = Read(18 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.pos = Vector3.new(X, Y, Z)
		-- Read 2
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_4)
		Item_1.pet = buffer.readstring(RecieveBuffer, Read(Length), Length)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_PetDropRemove(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_CardDropSpawn(): ({ { id: number, pos: Vector3 } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 16 bytes
	local ARRAY_START_1 = Read(16 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.pos = Vector3.new(X, Y, Z)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_CardDropRemove(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_HealthSpawn(): ({ { id: number, pos: Vector3 } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 16 bytes
	local ARRAY_START_1 = Read(16 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.pos = Vector3.new(X, Y, Z)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_HealthRemove(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_TixSpawn(): ({ { id: number, pos: Vector3, amount: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 20 bytes
	local ARRAY_START_1 = Read(20 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		Item_1.pos = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.amount = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_4)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_TixRemove(): ({ number })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 4 bytes
	local ARRAY_START_1 = Read(4 * Length)
	for Index = 1, Length do
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Item_1 = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_TixCount(): (number)
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Value = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_Profile(): ({ tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number })
	-- Read BLOCK: 20 bytes
	local BLOCK_START = Read(20)
	local Value = {} :: any
	Value.tix = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
	Value.bestWave = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
	Value.bestLevel = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
	Value.bestTime = buffer.readu32(RecieveBuffer, BLOCK_START + 8)
	Value.totalKills = buffer.readu32(RecieveBuffer, BLOCK_START + 12)
	Value.runs = buffer.readu32(RecieveBuffer, BLOCK_START + 16)
	return Value
end

function SerdesFunctions.ReadEVENT_UfoMove(): ({ { id: number, pos: Vector3 } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 10 bytes
	local ARRAY_START_1 = Read(10 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		local Encoded = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_1)
		local X
		local MantissaExponent = Encoded % 0x8000
		if MantissaExponent == 0b0_11111_0000000000  then
			if Encoded // 0x8000 == 1 then
				X = -math.huge
			else
				X = math.huge
			end
		elseif MantissaExponent == 0b1_11111_0000000000   then
			X = 0 / 0
		elseif MantissaExponent == 0b0_00000_0000000000   then
			X = 0
		else
			local Mantissa = MantissaExponent % 0x400
			local Exponent = MantissaExponent // 0x400
			local Fraction;
			if Exponent == 0 then
				Fraction = Mantissa / 0x400
			else
				Fraction = Mantissa / 0x800 + 0.5
			end
			local Result = math.ldexp(Fraction, Exponent - 14)
			X = if Encoded // 0x8000 == 1 then -Result else Result
		end
		-- Read 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		Encoded = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_2)
		local Y
		local MantissaExponent = Encoded % 0x8000
		if MantissaExponent == 0b0_11111_0000000000  then
			if Encoded // 0x8000 == 1 then
				Y = -math.huge
			else
				Y = math.huge
			end
		elseif MantissaExponent == 0b1_11111_0000000000   then
			Y = 0 / 0
		elseif MantissaExponent == 0b0_00000_0000000000   then
			Y = 0
		else
			local Mantissa = MantissaExponent % 0x400
			local Exponent = MantissaExponent // 0x400
			local Fraction;
			if Exponent == 0 then
				Fraction = Mantissa / 0x400
			else
				Fraction = Mantissa / 0x800 + 0.5
			end
			local Result = math.ldexp(Fraction, Exponent - 14)
			Y = if Encoded // 0x8000 == 1 then -Result else Result
		end
		-- Read 2
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 2
		Encoded = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_3)
		local Z
		local MantissaExponent = Encoded % 0x8000
		if MantissaExponent == 0b0_11111_0000000000  then
			if Encoded // 0x8000 == 1 then
				Z = -math.huge
			else
				Z = math.huge
			end
		elseif MantissaExponent == 0b1_11111_0000000000   then
			Z = 0 / 0
		elseif MantissaExponent == 0b0_00000_0000000000   then
			Z = 0
		else
			local Mantissa = MantissaExponent % 0x400
			local Exponent = MantissaExponent // 0x400
			local Fraction;
			if Exponent == 0 then
				Fraction = Mantissa / 0x400
			else
				Fraction = Mantissa / 0x800 + 0.5
			end
			local Result = math.ldexp(Fraction, Exponent - 14)
			Z = if Encoded // 0x8000 == 1 then -Result else Result
		end
		Item_1.pos = Vector3.new(X, Y, Z)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UfoFire(): ({ { origin: Vector3, vel: Vector3, t0: number, life: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 36 bytes
	local ARRAY_START_1 = Read(36 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
		Item_1.origin = Vector3.new(X, Y, Z)
		-- Read 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
		-- Read 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
		Item_1.vel = Vector3.new(X, Y, Z)
		-- Read 8
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 8
		Item_1.t0 = buffer.readf64(RecieveBuffer, OPERATION_OFFSET_6)
		-- Read 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.life = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_7)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_UfoBeam(): ({ { id: number, target: Vector3, charging: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 11 bytes
	local ARRAY_START_1 = Read(11 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		Item_1.id = buffer.readu32(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		local Encoded = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_1)
		local X
		local MantissaExponent = Encoded % 0x8000
		if MantissaExponent == 0b0_11111_0000000000  then
			if Encoded // 0x8000 == 1 then
				X = -math.huge
			else
				X = math.huge
			end
		elseif MantissaExponent == 0b1_11111_0000000000   then
			X = 0 / 0
		elseif MantissaExponent == 0b0_00000_0000000000   then
			X = 0
		else
			local Mantissa = MantissaExponent % 0x400
			local Exponent = MantissaExponent // 0x400
			local Fraction;
			if Exponent == 0 then
				Fraction = Mantissa / 0x400
			else
				Fraction = Mantissa / 0x800 + 0.5
			end
			local Result = math.ldexp(Fraction, Exponent - 14)
			X = if Encoded // 0x8000 == 1 then -Result else Result
		end
		-- Read 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		Encoded = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_2)
		local Y
		local MantissaExponent = Encoded % 0x8000
		if MantissaExponent == 0b0_11111_0000000000  then
			if Encoded // 0x8000 == 1 then
				Y = -math.huge
			else
				Y = math.huge
			end
		elseif MantissaExponent == 0b1_11111_0000000000   then
			Y = 0 / 0
		elseif MantissaExponent == 0b0_00000_0000000000   then
			Y = 0
		else
			local Mantissa = MantissaExponent % 0x400
			local Exponent = MantissaExponent // 0x400
			local Fraction;
			if Exponent == 0 then
				Fraction = Mantissa / 0x400
			else
				Fraction = Mantissa / 0x800 + 0.5
			end
			local Result = math.ldexp(Fraction, Exponent - 14)
			Y = if Encoded // 0x8000 == 1 then -Result else Result
		end
		-- Read 2
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 2
		Encoded = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_3)
		local Z
		local MantissaExponent = Encoded % 0x8000
		if MantissaExponent == 0b0_11111_0000000000  then
			if Encoded // 0x8000 == 1 then
				Z = -math.huge
			else
				Z = math.huge
			end
		elseif MantissaExponent == 0b1_11111_0000000000   then
			Z = 0 / 0
		elseif MantissaExponent == 0b0_00000_0000000000   then
			Z = 0
		else
			local Mantissa = MantissaExponent % 0x400
			local Exponent = MantissaExponent // 0x400
			local Fraction;
			if Exponent == 0 then
				Fraction = Mantissa / 0x400
			else
				Fraction = Mantissa / 0x800 + 0.5
			end
			local Result = math.ldexp(Fraction, Exponent - 14)
			Z = if Encoded // 0x8000 == 1 then -Result else Result
		end
		Item_1.target = Vector3.new(X, Y, Z)
		-- Read 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.charging = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_4)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	PlayerAction = {
		Fire = NOOP
	},
	UpdateTransformDelta = {
		On = NOOP
	},
	UpdateTransformFull = {
		On = NOOP
	},
	Spawn = {
		On = NOOP
	},
	UpdateMobState = {
		On = NOOP
	},
	UpdateMobPath = {
		On = NOOP
	},
	UpdateMobChase = {
		On = NOOP
	},
	StatSheet = {
		On = NOOP
	},
	MobHealth = {
		On = NOOP
	},
	Death = {
		On = NOOP
	},
	FireBubble = {
		On = NOOP
	},
	RemoveBubble = {
		On = NOOP
	},
	SpawnExp = {
		On = NOOP
	},
	RemoveExp = {
		On = NOOP
	},
	LevelUp = {
		On = NOOP
	},
	ChooseCard = {
		Fire = NOOP
	},
	Aim = {
		Fire = NOOP
	},
	UseSkill = {
		Fire = NOOP
	},
	GameOver = {
		On = NOOP
	},
	Restart = {
		Fire = NOOP
	},
	SwitchPlace = {
		Fire = NOOP
	},
	RoundOver = {
		On = NOOP
	},
	Weapons = {
		On = NOOP
	},
	Unlocked = {
		On = NOOP
	},
	SwitchWeapons = {
		Fire = NOOP
	},
	SetMain = {
		Fire = NOOP
	},
	BuyPet = {
		Fire = NOOP
	},
	Items = {
		On = NOOP
	},
	MainWeapon = {
		On = NOOP
	},
	Effect = {
		On = NOOP
	},
	SummonSpawn = {
		On = NOOP
	},
	SummonRemove = {
		On = NOOP
	},
	HazardSpawn = {
		On = NOOP
	},
	HazardRemove = {
		On = NOOP
	},
	UltBeam = {
		On = NOOP
	},
	WaveStart = {
		On = NOOP
	},
	Stats = {
		On = NOOP
	},
	Hit = {
		On = NOOP
	},
	Toast = {
		On = NOOP
	},
	PetDropSpawn = {
		On = NOOP
	},
	PetDropRemove = {
		On = NOOP
	},
	CardDropSpawn = {
		On = NOOP
	},
	CardDropRemove = {
		On = NOOP
	},
	HealthSpawn = {
		On = NOOP
	},
	HealthRemove = {
		On = NOOP
	},
	TixSpawn = {
		On = NOOP
	},
	TixRemove = {
		On = NOOP
	},
	TixCount = {
		On = NOOP
	},
	Profile = {
		On = NOOP
	},
	UfoMove = {
		On = NOOP
	},
	UfoFire = {
		On = NOOP
	},
	UfoBeam = {
		On = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
    error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
    if SendCursor <= 0 then
        return
    end

    local Buffer = buffer.create(SendCursor)
    buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
    Reliable:FireServer(Buffer, SendInstances)

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    table.clear(SendInstances)
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 1 then
			local Value: { { id: number, dp: Vector3 } } = SerdesFunctions.ReadEVENT_UpdateTransformFull()
			if Events.Reliable[1] ~= nil then
				SerdesEvent = "UpdateTransformFull"
				Events.Reliable[1](Value)
			else
				if #Queue.Reliable[1] > 256 then
					warn("[Blink]: Event queue of \"UpdateTransformFull\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[1], {Value} :: {any})
			end
		elseif Index == 2 then
			local Value: { { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } } = SerdesFunctions.ReadEVENT_Spawn()
			if #Events.Reliable[2] > 0 then
				for _, Listener in Events.Reliable[2] do
					SerdesEvent = "Spawn"
					Listener(Value)
				end
			else
				if #Queue.Reliable[2] > 256 then
					warn("[Blink]: Event queue of \"Spawn\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[2], {Value} :: {any})
			end
		elseif Index == 3 then
			local Value: { { id: number, state: {[string]: string} } } = SerdesFunctions.ReadEVENT_UpdateMobState()
			if Events.Reliable[3] ~= nil then
				SerdesEvent = "UpdateMobState"
				Events.Reliable[3](Value)
			else
				if #Queue.Reliable[3] > 256 then
					warn("[Blink]: Event queue of \"UpdateMobState\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[3], {Value} :: {any})
			end
		elseif Index == 4 then
			local Value: { { id: number, from: number, to: number, speed: number, startTime: number } } = SerdesFunctions.ReadEVENT_UpdateMobPath()
			if Events.Reliable[4] ~= nil then
				SerdesEvent = "UpdateMobPath"
				Events.Reliable[4](Value)
			else
				if #Queue.Reliable[4] > 256 then
					warn("[Blink]: Event queue of \"UpdateMobPath\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[4], {Value} :: {any})
			end
		elseif Index == 5 then
			local Value: { { id: number, target: Instance, speed: number } } = SerdesFunctions.ReadEVENT_UpdateMobChase()
			if Events.Reliable[5] ~= nil then
				SerdesEvent = "UpdateMobChase"
				Events.Reliable[5](Value)
			else
				if #Queue.Reliable[5] > 256 then
					warn("[Blink]: Event queue of \"UpdateMobChase\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[5], {Value} :: {any})
			end
		elseif Index == 6 then
			local Value: { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number } = SerdesFunctions.ReadEVENT_StatSheet()
			if Events.Reliable[6] ~= nil then
				SerdesEvent = "StatSheet"
				Events.Reliable[6](Value)
			else
				if #Queue.Reliable[6] > 256 then
					warn("[Blink]: Event queue of \"StatSheet\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[6], {Value} :: {any})
			end
		elseif Index == 7 then
			local Value: { { id: number, hp: number, max: number } } = SerdesFunctions.ReadEVENT_MobHealth()
			if #Events.Reliable[7] > 0 then
				for _, Listener in Events.Reliable[7] do
					SerdesEvent = "MobHealth"
					Listener(Value)
				end
			else
				if #Queue.Reliable[7] > 256 then
					warn("[Blink]: Event queue of \"MobHealth\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[7], {Value} :: {any})
			end
		elseif Index == 8 then
			local Value: { number } = SerdesFunctions.ReadEVENT_Death()
			if #Events.Reliable[8] > 0 then
				for _, Listener in Events.Reliable[8] do
					SerdesEvent = "Death"
					Listener(Value)
				end
			else
				if #Queue.Reliable[8] > 256 then
					warn("[Blink]: Event queue of \"Death\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[8], {Value} :: {any})
			end
		elseif Index == 9 then
			local Value: { { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } } = SerdesFunctions.ReadEVENT_FireBubble()
			if Events.Reliable[9] ~= nil then
				SerdesEvent = "FireBubble"
				Events.Reliable[9](Value)
			else
				if #Queue.Reliable[9] > 256 then
					warn("[Blink]: Event queue of \"FireBubble\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[9], {Value} :: {any})
			end
		elseif Index == 10 then
			local Value: { { id: number, pos: Vector3, tier: number } } = SerdesFunctions.ReadEVENT_SpawnExp()
			if Events.Reliable[10] ~= nil then
				SerdesEvent = "SpawnExp"
				Events.Reliable[10](Value)
			else
				if #Queue.Reliable[10] > 256 then
					warn("[Blink]: Event queue of \"SpawnExp\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[10], {Value} :: {any})
			end
		elseif Index == 11 then
			local Value: { number } = SerdesFunctions.ReadEVENT_RemoveExp()
			if Events.Reliable[11] ~= nil then
				SerdesEvent = "RemoveExp"
				Events.Reliable[11](Value)
			else
				if #Queue.Reliable[11] > 256 then
					warn("[Blink]: Event queue of \"RemoveExp\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[11], {Value} :: {any})
			end
		elseif Index == 12 then
			local Value: { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } } = SerdesFunctions.ReadEVENT_LevelUp()
			if Events.Reliable[12] ~= nil then
				SerdesEvent = "LevelUp"
				Events.Reliable[12](Value)
			else
				if #Queue.Reliable[12] > 256 then
					warn("[Blink]: Event queue of \"LevelUp\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[12], {Value} :: {any})
			end
		elseif Index == 15 then
			local Value: { time: number, level: number } = SerdesFunctions.ReadEVENT_GameOver()
			if Events.Reliable[15] ~= nil then
				SerdesEvent = "GameOver"
				Events.Reliable[15](Value)
			else
				if #Queue.Reliable[15] > 256 then
					warn("[Blink]: Event queue of \"GameOver\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[15], {Value} :: {any})
			end
		elseif Index == 18 then
			local Value: number = SerdesFunctions.ReadEVENT_RoundOver()
			if #Events.Reliable[18] > 0 then
				for _, Listener in Events.Reliable[18] do
					SerdesEvent = "RoundOver"
					Listener(Value)
				end
			else
				if #Queue.Reliable[18] > 256 then
					warn("[Blink]: Event queue of \"RoundOver\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[18], {Value} :: {any})
			end
		elseif Index == 19 then
			local Value: { { id: string, level: number } } = SerdesFunctions.ReadEVENT_Weapons()
			if #Events.Reliable[19] > 0 then
				for _, Listener in Events.Reliable[19] do
					SerdesEvent = "Weapons"
					Listener(Value)
				end
			else
				if #Queue.Reliable[19] > 256 then
					warn("[Blink]: Event queue of \"Weapons\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[19], {Value} :: {any})
			end
		elseif Index == 20 then
			local Value: { string } = SerdesFunctions.ReadEVENT_Unlocked()
			if Events.Reliable[20] ~= nil then
				SerdesEvent = "Unlocked"
				Events.Reliable[20](Value)
			else
				if #Queue.Reliable[20] > 256 then
					warn("[Blink]: Event queue of \"Unlocked\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[20], {Value} :: {any})
			end
		elseif Index == 24 then
			local Value: { { id: string, level: number } } = SerdesFunctions.ReadEVENT_Items()
			if #Events.Reliable[24] > 0 then
				for _, Listener in Events.Reliable[24] do
					SerdesEvent = "Items"
					Listener(Value)
				end
			else
				if #Queue.Reliable[24] > 256 then
					warn("[Blink]: Event queue of \"Items\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[24], {Value} :: {any})
			end
		elseif Index == 25 then
			local Value: number = SerdesFunctions.ReadEVENT_MainWeapon()
			if #Events.Reliable[25] > 0 then
				for _, Listener in Events.Reliable[25] do
					SerdesEvent = "MainWeapon"
					Listener(Value)
				end
			else
				if #Queue.Reliable[25] > 256 then
					warn("[Blink]: Event queue of \"MainWeapon\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[25], {Value} :: {any})
			end
		elseif Index == 26 then
			local Value: { { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } } = SerdesFunctions.ReadEVENT_Effect()
			if Events.Reliable[26] ~= nil then
				SerdesEvent = "Effect"
				Events.Reliable[26](Value)
			else
				if #Queue.Reliable[26] > 256 then
					warn("[Blink]: Event queue of \"Effect\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[26], {Value} :: {any})
			end
		elseif Index == 27 then
			local Value: { { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } } = SerdesFunctions.ReadEVENT_SummonSpawn()
			if Events.Reliable[27] ~= nil then
				SerdesEvent = "SummonSpawn"
				Events.Reliable[27](Value)
			else
				if #Queue.Reliable[27] > 256 then
					warn("[Blink]: Event queue of \"SummonSpawn\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[27], {Value} :: {any})
			end
		elseif Index == 28 then
			local Value: { number } = SerdesFunctions.ReadEVENT_SummonRemove()
			if Events.Reliable[28] ~= nil then
				SerdesEvent = "SummonRemove"
				Events.Reliable[28](Value)
			else
				if #Queue.Reliable[28] > 256 then
					warn("[Blink]: Event queue of \"SummonRemove\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[28], {Value} :: {any})
			end
		elseif Index == 29 then
			local Value: { { id: number, kind: number, pos: Vector3, radius: number, life: number } } = SerdesFunctions.ReadEVENT_HazardSpawn()
			if Events.Reliable[29] ~= nil then
				SerdesEvent = "HazardSpawn"
				Events.Reliable[29](Value)
			else
				if #Queue.Reliable[29] > 256 then
					warn("[Blink]: Event queue of \"HazardSpawn\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[29], {Value} :: {any})
			end
		elseif Index == 30 then
			local Value: { number } = SerdesFunctions.ReadEVENT_HazardRemove()
			if Events.Reliable[30] ~= nil then
				SerdesEvent = "HazardRemove"
				Events.Reliable[30](Value)
			else
				if #Queue.Reliable[30] > 256 then
					warn("[Blink]: Event queue of \"HazardRemove\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[30], {Value} :: {any})
			end
		elseif Index == 31 then
			local Value: { active: number, life: number, width: number, length: number } = SerdesFunctions.ReadEVENT_UltBeam()
			if Events.Reliable[31] ~= nil then
				SerdesEvent = "UltBeam"
				Events.Reliable[31](Value)
			else
				if #Queue.Reliable[31] > 256 then
					warn("[Blink]: Event queue of \"UltBeam\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[31], {Value} :: {any})
			end
		elseif Index == 32 then
			local Value: number = SerdesFunctions.ReadEVENT_WaveStart()
			if Events.Reliable[32] ~= nil then
				SerdesEvent = "WaveStart"
				Events.Reliable[32](Value)
			else
				if #Queue.Reliable[32] > 256 then
					warn("[Blink]: Event queue of \"WaveStart\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[32], {Value} :: {any})
			end
		elseif Index == 33 then
			local Value: { level: number, xp: number, xpNext: number, hp: number, maxHp: number } = SerdesFunctions.ReadEVENT_Stats()
			if #Events.Reliable[33] > 0 then
				for _, Listener in Events.Reliable[33] do
					SerdesEvent = "Stats"
					Listener(Value)
				end
			else
				if #Queue.Reliable[33] > 256 then
					warn("[Blink]: Event queue of \"Stats\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[33], {Value} :: {any})
			end
		elseif Index == 34 then
			local Value: string = SerdesFunctions.ReadEVENT_Toast()
			if Events.Reliable[34] ~= nil then
				SerdesEvent = "Toast"
				Events.Reliable[34](Value)
			else
				if #Queue.Reliable[34] > 256 then
					warn("[Blink]: Event queue of \"Toast\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[34], {Value} :: {any})
			end
		elseif Index == 35 then
			local Value: { { id: number, pos: Vector3, pet: string } } = SerdesFunctions.ReadEVENT_PetDropSpawn()
			if Events.Reliable[35] ~= nil then
				SerdesEvent = "PetDropSpawn"
				Events.Reliable[35](Value)
			else
				if #Queue.Reliable[35] > 256 then
					warn("[Blink]: Event queue of \"PetDropSpawn\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[35], {Value} :: {any})
			end
		elseif Index == 36 then
			local Value: { number } = SerdesFunctions.ReadEVENT_PetDropRemove()
			if Events.Reliable[36] ~= nil then
				SerdesEvent = "PetDropRemove"
				Events.Reliable[36](Value)
			else
				if #Queue.Reliable[36] > 256 then
					warn("[Blink]: Event queue of \"PetDropRemove\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[36], {Value} :: {any})
			end
		elseif Index == 37 then
			local Value: { { id: number, pos: Vector3 } } = SerdesFunctions.ReadEVENT_CardDropSpawn()
			if Events.Reliable[37] ~= nil then
				SerdesEvent = "CardDropSpawn"
				Events.Reliable[37](Value)
			else
				if #Queue.Reliable[37] > 256 then
					warn("[Blink]: Event queue of \"CardDropSpawn\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[37], {Value} :: {any})
			end
		elseif Index == 38 then
			local Value: { number } = SerdesFunctions.ReadEVENT_CardDropRemove()
			if Events.Reliable[38] ~= nil then
				SerdesEvent = "CardDropRemove"
				Events.Reliable[38](Value)
			else
				if #Queue.Reliable[38] > 256 then
					warn("[Blink]: Event queue of \"CardDropRemove\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[38], {Value} :: {any})
			end
		elseif Index == 39 then
			local Value: { { id: number, pos: Vector3 } } = SerdesFunctions.ReadEVENT_HealthSpawn()
			if Events.Reliable[39] ~= nil then
				SerdesEvent = "HealthSpawn"
				Events.Reliable[39](Value)
			else
				if #Queue.Reliable[39] > 256 then
					warn("[Blink]: Event queue of \"HealthSpawn\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[39], {Value} :: {any})
			end
		elseif Index == 40 then
			local Value: { number } = SerdesFunctions.ReadEVENT_HealthRemove()
			if Events.Reliable[40] ~= nil then
				SerdesEvent = "HealthRemove"
				Events.Reliable[40](Value)
			else
				if #Queue.Reliable[40] > 256 then
					warn("[Blink]: Event queue of \"HealthRemove\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[40], {Value} :: {any})
			end
		elseif Index == 41 then
			local Value: { { id: number, pos: Vector3, amount: number } } = SerdesFunctions.ReadEVENT_TixSpawn()
			if Events.Reliable[41] ~= nil then
				SerdesEvent = "TixSpawn"
				Events.Reliable[41](Value)
			else
				if #Queue.Reliable[41] > 256 then
					warn("[Blink]: Event queue of \"TixSpawn\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[41], {Value} :: {any})
			end
		elseif Index == 42 then
			local Value: { number } = SerdesFunctions.ReadEVENT_TixRemove()
			if Events.Reliable[42] ~= nil then
				SerdesEvent = "TixRemove"
				Events.Reliable[42](Value)
			else
				if #Queue.Reliable[42] > 256 then
					warn("[Blink]: Event queue of \"TixRemove\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[42], {Value} :: {any})
			end
		elseif Index == 43 then
			local Value: number = SerdesFunctions.ReadEVENT_TixCount()
			if #Events.Reliable[43] > 0 then
				for _, Listener in Events.Reliable[43] do
					SerdesEvent = "TixCount"
					Listener(Value)
				end
			else
				if #Queue.Reliable[43] > 256 then
					warn("[Blink]: Event queue of \"TixCount\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[43], {Value} :: {any})
			end
		elseif Index == 44 then
			local Value: { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number } = SerdesFunctions.ReadEVENT_Profile()
			if Events.Reliable[44] ~= nil then
				SerdesEvent = "Profile"
				Events.Reliable[44](Value)
			else
				if #Queue.Reliable[44] > 256 then
					warn("[Blink]: Event queue of \"Profile\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[44], {Value} :: {any})
			end
		elseif Index == 45 then
			local Value: { { origin: Vector3, vel: Vector3, t0: number, life: number } } = SerdesFunctions.ReadEVENT_UfoFire()
			if Events.Reliable[45] ~= nil then
				SerdesEvent = "UfoFire"
				Events.Reliable[45](Value)
			else
				if #Queue.Reliable[45] > 256 then
					warn("[Blink]: Event queue of \"UfoFire\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[45], {Value} :: {any})
			end
		end
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: { base: number, deltas: { number } } = SerdesFunctions.ReadEVENT_UpdateTransformDelta()
			if Events.Unreliable[0] ~= nil then
				SerdesEvent = "UpdateTransformDelta"
				Events.Unreliable[0](Value)
			end
		elseif Index == 1 then
			local Value: { number } = SerdesFunctions.ReadEVENT_RemoveBubble()
			if Events.Unreliable[1] ~= nil then
				SerdesEvent = "RemoveBubble"
				Events.Unreliable[1](Value)
			end
		elseif Index == 3 then
			local Value: { number } = SerdesFunctions.ReadEVENT_Hit()
			for _, Listener in Events.Unreliable[3] do
				SerdesEvent = "Hit"
				Listener(Value)
			end
		elseif Index == 4 then
			local Value: { { id: number, pos: Vector3 } } = SerdesFunctions.ReadEVENT_UfoMove()
			if Events.Unreliable[4] ~= nil then
				SerdesEvent = "UfoMove"
				Events.Unreliable[4](Value)
			end
		elseif Index == 5 then
			local Value: { { id: number, target: Vector3, charging: number } } = SerdesFunctions.ReadEVENT_UfoBeam()
			if Events.Unreliable[5] ~= nil then
				SerdesEvent = "UfoBeam"
				Events.Unreliable[5](Value)
			end
		end
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	PlayerAction = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_PlayerAction(Value)
		end
	},
	UpdateTransformDelta = {
		On = function(Listener: (Value: { base: number, deltas: { number } }) -> ()): () -> ()
			Events.Unreliable[0] = Listener
			return function (): ()
				if Events.Unreliable[0] == Listener then
					Events.Unreliable[0] = nil
				end
			end
		end
	},
	UpdateTransformFull = {
		On = function(Listener: (Value: { { id: number, dp: Vector3 } }) -> ()): () -> ()
			Events.Reliable[1] = Listener
			for Index, Arguments in Queue.Reliable[1] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[1] = {}
			return function (): ()
				if Events.Reliable[1] == Listener then
					Events.Reliable[1] = nil
				end
			end
		end
	},
	Spawn = {
		On = function(Listener: (Value: { { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } }) -> ()): () -> ()
			table.insert(Events.Reliable[2], Listener)
			for Index, Arguments in Queue.Reliable[2] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[2] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[2], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[2], Index)
				end
			end
		end
	},
	UpdateMobState = {
		On = function(Listener: (Value: { { id: number, state: {[string]: string} } }) -> ()): () -> ()
			Events.Reliable[3] = Listener
			for Index, Arguments in Queue.Reliable[3] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[3] = {}
			return function (): ()
				if Events.Reliable[3] == Listener then
					Events.Reliable[3] = nil
				end
			end
		end
	},
	UpdateMobPath = {
		On = function(Listener: (Value: { { id: number, from: number, to: number, speed: number, startTime: number } }) -> ()): () -> ()
			Events.Reliable[4] = Listener
			for Index, Arguments in Queue.Reliable[4] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[4] = {}
			return function (): ()
				if Events.Reliable[4] == Listener then
					Events.Reliable[4] = nil
				end
			end
		end
	},
	UpdateMobChase = {
		On = function(Listener: (Value: { { id: number, target: Instance, speed: number } }) -> ()): () -> ()
			Events.Reliable[5] = Listener
			for Index, Arguments in Queue.Reliable[5] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[5] = {}
			return function (): ()
				if Events.Reliable[5] == Listener then
					Events.Reliable[5] = nil
				end
			end
		end
	},
	StatSheet = {
		On = function(Listener: (Value: { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }) -> ()): () -> ()
			Events.Reliable[6] = Listener
			for Index, Arguments in Queue.Reliable[6] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[6] = {}
			return function (): ()
				if Events.Reliable[6] == Listener then
					Events.Reliable[6] = nil
				end
			end
		end
	},
	MobHealth = {
		On = function(Listener: (Value: { { id: number, hp: number, max: number } }) -> ()): () -> ()
			table.insert(Events.Reliable[7], Listener)
			for Index, Arguments in Queue.Reliable[7] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[7] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[7], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[7], Index)
				end
			end
		end
	},
	Death = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			table.insert(Events.Reliable[8], Listener)
			for Index, Arguments in Queue.Reliable[8] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[8] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[8], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[8], Index)
				end
			end
		end
	},
	FireBubble = {
		On = function(Listener: (Value: { { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } }) -> ()): () -> ()
			Events.Reliable[9] = Listener
			for Index, Arguments in Queue.Reliable[9] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[9] = {}
			return function (): ()
				if Events.Reliable[9] == Listener then
					Events.Reliable[9] = nil
				end
			end
		end
	},
	RemoveBubble = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Unreliable[1] = Listener
			return function (): ()
				if Events.Unreliable[1] == Listener then
					Events.Unreliable[1] = nil
				end
			end
		end
	},
	SpawnExp = {
		On = function(Listener: (Value: { { id: number, pos: Vector3, tier: number } }) -> ()): () -> ()
			Events.Reliable[10] = Listener
			for Index, Arguments in Queue.Reliable[10] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[10] = {}
			return function (): ()
				if Events.Reliable[10] == Listener then
					Events.Reliable[10] = nil
				end
			end
		end
	},
	RemoveExp = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[11] = Listener
			for Index, Arguments in Queue.Reliable[11] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[11] = {}
			return function (): ()
				if Events.Reliable[11] == Listener then
					Events.Reliable[11] = nil
				end
			end
		end
	},
	LevelUp = {
		On = function(Listener: (Value: { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }) -> ()): () -> ()
			Events.Reliable[12] = Listener
			for Index, Arguments in Queue.Reliable[12] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[12] = {}
			return function (): ()
				if Events.Reliable[12] == Listener then
					Events.Reliable[12] = nil
				end
			end
		end
	},
	ChooseCard = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_ChooseCard(Value)
		end
	},
	Aim = {
		Fire = function(Value: Vector3): ()
			local Previous = Save()
			Load()
			SerdesFunctions.WriteEVENT_Aim(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireServer(Buffer, SendInstances)
			Load(Previous)
		end
	},
	UseSkill = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_UseSkill(Value)
		end
	},
	GameOver = {
		On = function(Listener: (Value: { time: number, level: number }) -> ()): () -> ()
			Events.Reliable[15] = Listener
			for Index, Arguments in Queue.Reliable[15] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[15] = {}
			return function (): ()
				if Events.Reliable[15] == Listener then
					Events.Reliable[15] = nil
				end
			end
		end
	},
	Restart = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_Restart(Value)
		end
	},
	SwitchPlace = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_SwitchPlace(Value)
		end
	},
	RoundOver = {
		On = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[18], Listener)
			for Index, Arguments in Queue.Reliable[18] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[18] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[18], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[18], Index)
				end
			end
		end
	},
	Weapons = {
		On = function(Listener: (Value: { { id: string, level: number } }) -> ()): () -> ()
			table.insert(Events.Reliable[19], Listener)
			for Index, Arguments in Queue.Reliable[19] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[19] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[19], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[19], Index)
				end
			end
		end
	},
	Unlocked = {
		On = function(Listener: (Value: { string }) -> ()): () -> ()
			Events.Reliable[20] = Listener
			for Index, Arguments in Queue.Reliable[20] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[20] = {}
			return function (): ()
				if Events.Reliable[20] == Listener then
					Events.Reliable[20] = nil
				end
			end
		end
	},
	SwitchWeapons = {
		Fire = function(Value: { string }): ()
			SerdesFunctions.WriteEVENT_SwitchWeapons(Value)
		end
	},
	SetMain = {
		Fire = function(Value: string): ()
			SerdesFunctions.WriteEVENT_SetMain(Value)
		end
	},
	BuyPet = {
		Fire = function(Value: string): ()
			SerdesFunctions.WriteEVENT_BuyPet(Value)
		end
	},
	Items = {
		On = function(Listener: (Value: { { id: string, level: number } }) -> ()): () -> ()
			table.insert(Events.Reliable[24], Listener)
			for Index, Arguments in Queue.Reliable[24] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[24] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[24], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[24], Index)
				end
			end
		end
	},
	MainWeapon = {
		On = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[25], Listener)
			for Index, Arguments in Queue.Reliable[25] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[25] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[25], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[25], Index)
				end
			end
		end
	},
	Effect = {
		On = function(Listener: (Value: { { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } }) -> ()): () -> ()
			Events.Reliable[26] = Listener
			for Index, Arguments in Queue.Reliable[26] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[26] = {}
			return function (): ()
				if Events.Reliable[26] == Listener then
					Events.Reliable[26] = nil
				end
			end
		end
	},
	SummonSpawn = {
		On = function(Listener: (Value: { { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } }) -> ()): () -> ()
			Events.Reliable[27] = Listener
			for Index, Arguments in Queue.Reliable[27] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[27] = {}
			return function (): ()
				if Events.Reliable[27] == Listener then
					Events.Reliable[27] = nil
				end
			end
		end
	},
	SummonRemove = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[28] = Listener
			for Index, Arguments in Queue.Reliable[28] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[28] = {}
			return function (): ()
				if Events.Reliable[28] == Listener then
					Events.Reliable[28] = nil
				end
			end
		end
	},
	HazardSpawn = {
		On = function(Listener: (Value: { { id: number, kind: number, pos: Vector3, radius: number, life: number } }) -> ()): () -> ()
			Events.Reliable[29] = Listener
			for Index, Arguments in Queue.Reliable[29] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[29] = {}
			return function (): ()
				if Events.Reliable[29] == Listener then
					Events.Reliable[29] = nil
				end
			end
		end
	},
	HazardRemove = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[30] = Listener
			for Index, Arguments in Queue.Reliable[30] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[30] = {}
			return function (): ()
				if Events.Reliable[30] == Listener then
					Events.Reliable[30] = nil
				end
			end
		end
	},
	UltBeam = {
		On = function(Listener: (Value: { active: number, life: number, width: number, length: number }) -> ()): () -> ()
			Events.Reliable[31] = Listener
			for Index, Arguments in Queue.Reliable[31] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[31] = {}
			return function (): ()
				if Events.Reliable[31] == Listener then
					Events.Reliable[31] = nil
				end
			end
		end
	},
	WaveStart = {
		On = function(Listener: (Value: number) -> ()): () -> ()
			Events.Reliable[32] = Listener
			for Index, Arguments in Queue.Reliable[32] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[32] = {}
			return function (): ()
				if Events.Reliable[32] == Listener then
					Events.Reliable[32] = nil
				end
			end
		end
	},
	Stats = {
		On = function(Listener: (Value: { level: number, xp: number, xpNext: number, hp: number, maxHp: number }) -> ()): () -> ()
			table.insert(Events.Reliable[33], Listener)
			for Index, Arguments in Queue.Reliable[33] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[33] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[33], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[33], Index)
				end
			end
		end
	},
	Hit = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			table.insert(Events.Unreliable[3], Listener)
			return function (): ()
				local Index = table.find(Events.Unreliable[3], Listener)
				if Index ~= nil then
					table.remove(Events.Unreliable[3], Index)
				end
			end
		end
	},
	Toast = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			Events.Reliable[34] = Listener
			for Index, Arguments in Queue.Reliable[34] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[34] = {}
			return function (): ()
				if Events.Reliable[34] == Listener then
					Events.Reliable[34] = nil
				end
			end
		end
	},
	PetDropSpawn = {
		On = function(Listener: (Value: { { id: number, pos: Vector3, pet: string } }) -> ()): () -> ()
			Events.Reliable[35] = Listener
			for Index, Arguments in Queue.Reliable[35] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[35] = {}
			return function (): ()
				if Events.Reliable[35] == Listener then
					Events.Reliable[35] = nil
				end
			end
		end
	},
	PetDropRemove = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[36] = Listener
			for Index, Arguments in Queue.Reliable[36] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[36] = {}
			return function (): ()
				if Events.Reliable[36] == Listener then
					Events.Reliable[36] = nil
				end
			end
		end
	},
	CardDropSpawn = {
		On = function(Listener: (Value: { { id: number, pos: Vector3 } }) -> ()): () -> ()
			Events.Reliable[37] = Listener
			for Index, Arguments in Queue.Reliable[37] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[37] = {}
			return function (): ()
				if Events.Reliable[37] == Listener then
					Events.Reliable[37] = nil
				end
			end
		end
	},
	CardDropRemove = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[38] = Listener
			for Index, Arguments in Queue.Reliable[38] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[38] = {}
			return function (): ()
				if Events.Reliable[38] == Listener then
					Events.Reliable[38] = nil
				end
			end
		end
	},
	HealthSpawn = {
		On = function(Listener: (Value: { { id: number, pos: Vector3 } }) -> ()): () -> ()
			Events.Reliable[39] = Listener
			for Index, Arguments in Queue.Reliable[39] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[39] = {}
			return function (): ()
				if Events.Reliable[39] == Listener then
					Events.Reliable[39] = nil
				end
			end
		end
	},
	HealthRemove = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[40] = Listener
			for Index, Arguments in Queue.Reliable[40] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[40] = {}
			return function (): ()
				if Events.Reliable[40] == Listener then
					Events.Reliable[40] = nil
				end
			end
		end
	},
	TixSpawn = {
		On = function(Listener: (Value: { { id: number, pos: Vector3, amount: number } }) -> ()): () -> ()
			Events.Reliable[41] = Listener
			for Index, Arguments in Queue.Reliable[41] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[41] = {}
			return function (): ()
				if Events.Reliable[41] == Listener then
					Events.Reliable[41] = nil
				end
			end
		end
	},
	TixRemove = {
		On = function(Listener: (Value: { number }) -> ()): () -> ()
			Events.Reliable[42] = Listener
			for Index, Arguments in Queue.Reliable[42] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[42] = {}
			return function (): ()
				if Events.Reliable[42] == Listener then
					Events.Reliable[42] = nil
				end
			end
		end
	},
	TixCount = {
		On = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[43], Listener)
			for Index, Arguments in Queue.Reliable[43] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[43] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[43], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[43], Index)
				end
			end
		end
	},
	Profile = {
		On = function(Listener: (Value: { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }) -> ()): () -> ()
			Events.Reliable[44] = Listener
			for Index, Arguments in Queue.Reliable[44] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[44] = {}
			return function (): ()
				if Events.Reliable[44] == Listener then
					Events.Reliable[44] = nil
				end
			end
		end
	},
	UfoMove = {
		On = function(Listener: (Value: { { id: number, pos: Vector3 } }) -> ()): () -> ()
			Events.Unreliable[4] = Listener
			return function (): ()
				if Events.Unreliable[4] == Listener then
					Events.Unreliable[4] = nil
				end
			end
		end
	},
	UfoFire = {
		On = function(Listener: (Value: { { origin: Vector3, vel: Vector3, t0: number, life: number } }) -> ()): () -> ()
			Events.Reliable[45] = Listener
			for Index, Arguments in Queue.Reliable[45] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[45] = {}
			return function (): ()
				if Events.Reliable[45] == Listener then
					Events.Reliable[45] = nil
				end
			end
		end
	},
	UfoBeam = {
		On = function(Listener: (Value: { { id: number, target: Vector3, charging: number } }) -> ()): () -> ()
			Events.Unreliable[5] = Listener
			return function (): ()
				if Events.Unreliable[5] == Listener then
					Events.Unreliable[5] = nil
				end
			end
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL