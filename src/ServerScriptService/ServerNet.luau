--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.18.7 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
Queue.Reliable[0] = table.create(256)
Queue.Reliable[13] = table.create(256)
Queue.Unreliable[2] = table.create(256)
Queue.Reliable[14] = table.create(256)
Queue.Reliable[16] = table.create(256)
Queue.Reliable[17] = table.create(256)
Queue.Reliable[21] = table.create(256)
Queue.Reliable[22] = table.create(256)
Queue.Reliable[23] = table.create(256)
export type state = {[string]: string}
export type DeltaChunk = { base: number, deltas: { number } }
export type TransformFull = { id: number, dp: Vector3 }
export type MobState = { id: number, state: {[string]: string} }
export type MobSpawn = { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number }
export type Path = { id: number, from: number, to: number, speed: number, startTime: number }
export type Chase = { id: number, target: Instance, speed: number }
export type BubbleFire = { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? }
export type ExpSpawn = { id: number, pos: Vector3, tier: number }
export type Card = { id: number, title: string, desc: string, kind: number, weapon: string }
export type StatsMsg = { level: number, xp: number, xpNext: number, hp: number, maxHp: number }
export type StatSheetMsg = { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }
export type GameOverMsg = { time: number, level: number }
export type MobHp = { id: number, hp: number, max: number }
export type Offer = { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }
export type WeaponInfo = { id: string, level: number }
export type ItemMsg = { id: string, level: number }
export type EffectMsg = { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? }
export type HazardMsg = { id: number, kind: number, pos: Vector3, radius: number, life: number }
export type SummonMsg = { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number }
export type BeamState = { active: number, life: number, width: number, length: number }
export type PetDrop = { id: number, pos: Vector3, pet: string }
export type CardDrop = { id: number, pos: Vector3 }
export type HealthDrop = { id: number, pos: Vector3 }
export type TixDrop = { id: number, pos: Vector3, amount: number }
export type ProfileMsg = { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }
export type UfoPos = { id: number, pos: Vector3 }
export type UfoBolt = { origin: Vector3, vel: Vector3, t0: number, life: number }
export type UfoBeamMsg = { id: number, target: Vector3, charging: number }
function SerdesFunctions.ReadEVENT_PlayerAction(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.WriteEVENT_UpdateTransformDelta(Value: { base: number, deltas: { number } }): ()
	-- Allocate BLOCK: 5 bytes
	local BLOCK_START = Allocate(5)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Value.base)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 3, #Value.deltas)
		-- Allocate BLOCK: 1 bytes
	local ARRAY_START_1 = Allocate(1 * #Value.deltas)
	for Index = 1, #Value.deltas do
		local Item_1 = Value.deltas[Index]
		-- Allocate 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writei8(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UpdateTransformFull(Value: { { id: number, dp: Vector3 } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 16 bytes
	local ARRAY_START_1 = Allocate(16 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.dp
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_Spawn(Value: { { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 37 bytes
	local ARRAY_START_1 = Allocate(37 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Item_1.slot)
		local Vector = Item_1.pos
		-- Allocate 8
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_2, Vector.X)
		-- Allocate 8
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_3, Vector.Y)
		-- Allocate 8
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_4, Vector.Z)
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Item_1.size)
		local Length = #Item_1.type
		-- Allocate 2
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_6, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.type, Length)
		-- Allocate 1
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_7, Item_1.tier)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UpdateMobState(Value: { { id: number, state: {[string]: string} } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 3)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 6 bytes
	local ARRAY_START_1 = Allocate(6 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		local Elements_2 = 0
		for Key_2, Element_2 in Item_1.state do
			Elements_2 += 1
			local Length = #Key_2
			local OFFSET_0 = Allocate(2)
			buffer.writeu16(SendBuffer, OFFSET_0, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Key_2, Length)
			local Length = #Element_2
			local OFFSET_1 = Allocate(2)
			buffer.writeu16(SendBuffer, OFFSET_1, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Element_2, Length)
		end
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Elements_2)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UpdateMobPath(Value: { { id: number, from: number, to: number, speed: number, startTime: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 4)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 24 bytes
	local ARRAY_START_1 = Allocate(24 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_1, Item_1.from)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_2, Item_1.to)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Item_1.speed)
		-- Allocate 8
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_4, Item_1.startTime)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UpdateMobChase(Value: { { id: number, target: Instance, speed: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 5)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 8 bytes
	local ARRAY_START_1 = Allocate(8 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		table.insert(SendInstances, Item_1.target or Null)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Item_1.speed)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_StatSheet(Value: { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }): ()
	-- Allocate BLOCK: 51 bytes
	local BLOCK_START = Allocate(51)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 6)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Value.maxHp)
	buffer.writef32(SendBuffer, BLOCK_START + 3, Value.hpRegen)
	buffer.writef32(SendBuffer, BLOCK_START + 7, Value.atk)
	buffer.writef32(SendBuffer, BLOCK_START + 11, Value.walkSpeed)
	buffer.writef32(SendBuffer, BLOCK_START + 15, Value.crit)
	buffer.writef32(SendBuffer, BLOCK_START + 19, Value.critDmg)
	buffer.writef32(SendBuffer, BLOCK_START + 23, Value.pickup)
	buffer.writef32(SendBuffer, BLOCK_START + 27, Value.haste)
	buffer.writef32(SendBuffer, BLOCK_START + 31, Value.expMult)
	buffer.writef32(SendBuffer, BLOCK_START + 35, Value.tixRate)
	buffer.writef32(SendBuffer, BLOCK_START + 39, Value.atkSize)
	buffer.writef32(SendBuffer, BLOCK_START + 43, Value.knock)
	buffer.writef32(SendBuffer, BLOCK_START + 47, Value.skillCd)
end

function SerdesFunctions.WriteEVENT_MobHealth(Value: { { id: number, hp: number, max: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 7)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 8 bytes
	local ARRAY_START_1 = Allocate(8 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Item_1.hp)
		-- Allocate 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, Item_1.max)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_Death(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 8)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_FireBubble(Value: { { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 9)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 57 bytes
	local ARRAY_START_1 = Allocate(57 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.origin
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
		local Vector = Item_1.velocity
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_6, Vector.Z)
		-- Allocate 8
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_7, Item_1.t0)
		-- Allocate 4
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_8, Item_1.life)
		local Vector = Item_1.color
		-- Allocate 4
		local OPERATION_OFFSET_9 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_9, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_10 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_10, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_11 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_11, Vector.Z)
		-- Allocate 4
		local OPERATION_OFFSET_12 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_12, Item_1.size)
		-- Allocate 1
		local OPERATION_OFFSET_13 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_13, Item_1.shape ~= nil and 1 or 0)
		if Item_1.shape ~= nil then
			-- Allocate BLOCK: 1 bytes
			local BLOCK_START = Allocate(1)
			buffer.writeu8(SendBuffer, BLOCK_START + 0, Item_1.shape)
		end
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_RemoveBubble(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_SpawnExp(Value: { { id: number, pos: Vector3, tier: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 10)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 17 bytes
	local ARRAY_START_1 = Allocate(17 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.pos
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
		-- Allocate 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_4, Item_1.tier)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_RemoveExp(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 11)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_LevelUp(Value: { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }): ()
	-- Allocate BLOCK: 4 bytes
	local BLOCK_START = Allocate(4)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 12)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.source)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 2, #Value.cards)
		-- Allocate BLOCK: 8 bytes
	local ARRAY_START_1 = Allocate(8 * #Value.cards)
	for Index = 1, #Value.cards do
		local Item_1 = Value.cards[Index]
		-- Allocate 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Length = #Item_1.title
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.title, Length)
		local Length = #Item_1.desc
		-- Allocate 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.desc, Length)
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.kind)
		local Length = #Item_1.weapon
		-- Allocate 2
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_4, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.weapon, Length)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_ChooseCard(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_Aim(): (Vector3)
	-- Read BLOCK: 12 bytes
	local BLOCK_START = Read(12)
	local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
	local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
	local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
	local Value = Vector3.new(X, Y, Z)
	return Value
end

function SerdesFunctions.ReadEVENT_UseSkill(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.WriteEVENT_GameOver(Value: { time: number, level: number }): ()
	-- Allocate BLOCK: 7 bytes
	local BLOCK_START = Allocate(7)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 15)
	buffer.writef32(SendBuffer, BLOCK_START + 1, Value.time)
	buffer.writeu16(SendBuffer, BLOCK_START + 5, Value.level)
end

function SerdesFunctions.ReadEVENT_Restart(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.ReadEVENT_SwitchPlace(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

function SerdesFunctions.WriteEVENT_RoundOver(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 18)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_Weapons(Value: { { id: string, level: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 19)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 3 bytes
	local ARRAY_START_1 = Allocate(3 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		local Length = #Item_1.id
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.id, Length)
		-- Allocate 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_1, Item_1.level)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_Unlocked(Value: { string }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 20)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 2 bytes
	local ARRAY_START_1 = Allocate(2 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		local Length = #Item_1
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1, Length)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_SwitchWeapons(): ({ string })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 2 bytes
	local ARRAY_START_1 = Read(2 * Length)
	for Index = 1, Length do
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		local Item_1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.ReadEVENT_SetMain(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

function SerdesFunctions.ReadEVENT_BuyPet(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

function SerdesFunctions.WriteEVENT_Items(Value: { { id: string, level: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 24)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 3 bytes
	local ARRAY_START_1 = Allocate(3 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		local Length = #Item_1.id
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.id, Length)
		-- Allocate 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_1, Item_1.level)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_MainWeapon(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 25)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_Effect(Value: { { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 26)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 31 bytes
	local ARRAY_START_1 = Allocate(31 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1.kind)
		local Vector = Item_1.pos
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Item_1.radius)
		local Vector = Item_1.dir
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_6, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_7, Vector.Z)
		-- Allocate 1
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_8, Item_1.arc ~= nil and 1 or 0)
		if Item_1.arc ~= nil then
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			if Item_1.arc > 65504 then
				buffer.writeu16(SendBuffer, BLOCK_START + 0, 0b0_11111_0000000000)
			elseif Item_1.arc < -65504 then
				buffer.writeu16(SendBuffer, BLOCK_START + 0, 0b1_11111_0000000000)
			elseif Item_1.arc ~= Item_1.arc then
				buffer.writeu16(SendBuffer, BLOCK_START + 0, 0b1_11111_0000000001)
			elseif Item_1.arc == 0 then
				buffer.writeu16(SendBuffer, BLOCK_START + 0, 0)
			else
				local float = Item_1.arc
				local Abosulte = math.abs(float)
				local Interval = math.ldexp(1, math.floor(math.log(Abosulte, 2)) - 10)
				local RoundedValue = (Abosulte // Interval) * Interval
				local Fraction, Exponent = math.frexp(RoundedValue)
				Exponent += 14
				local Mantissa = math.round(if Exponent <= 0
					then Fraction * 0x400 / math.ldexp(1, math.abs(Exponent))
					else Fraction * 0x800) % 0x400
				local Result = Mantissa
					+ math.max(Exponent, 0) * 0x400
					+ if float < 0 then 0x8000 else 0
				buffer.writeu16(SendBuffer, BLOCK_START + 0, Result)
			end
		end
		-- Allocate 1
		local OPERATION_OFFSET_9 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_9, Item_1.color ~= nil and 1 or 0)
		if Item_1.color ~= nil then
			-- Allocate BLOCK: 12 bytes
			local BLOCK_START = Allocate(12)
			local Vector = Item_1.color
			buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
			buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
			buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
		end
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_SummonSpawn(Value: { { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 27)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 41 bytes
	local ARRAY_START_1 = Allocate(41 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		-- Allocate 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_1, Item_1.kind)
		local Vector = Item_1.origin
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Vector.Z)
		local Vector = Item_1.velocity
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_6, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_7, Vector.Z)
		-- Allocate 8
		local OPERATION_OFFSET_8 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_8, Item_1.t0)
		-- Allocate 4
		local OPERATION_OFFSET_9 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_9, Item_1.life)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_SummonRemove(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 28)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_HazardSpawn(Value: { { id: number, kind: number, pos: Vector3, radius: number, life: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 29)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 25 bytes
	local ARRAY_START_1 = Allocate(25 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		-- Allocate 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_1, Item_1.kind)
		local Vector = Item_1.pos
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Vector.Z)
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Item_1.radius)
		-- Allocate 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_6, Item_1.life)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_HazardRemove(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 30)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UltBeam(Value: { active: number, life: number, width: number, length: number }): ()
	-- Allocate BLOCK: 14 bytes
	local BLOCK_START = Allocate(14)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 31)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.active)
	buffer.writef32(SendBuffer, BLOCK_START + 2, Value.life)
	buffer.writef32(SendBuffer, BLOCK_START + 6, Value.width)
	buffer.writef32(SendBuffer, BLOCK_START + 10, Value.length)
end

function SerdesFunctions.WriteEVENT_WaveStart(Value: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 32)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_Stats(Value: { level: number, xp: number, xpNext: number, hp: number, maxHp: number }): ()
	-- Allocate BLOCK: 15 bytes
	local BLOCK_START = Allocate(15)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 33)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Value.level)
	buffer.writeu32(SendBuffer, BLOCK_START + 3, Value.xp)
	buffer.writeu32(SendBuffer, BLOCK_START + 7, Value.xpNext)
	buffer.writeu16(SendBuffer, BLOCK_START + 11, Value.hp)
	buffer.writeu16(SendBuffer, BLOCK_START + 13, Value.maxHp)
end

function SerdesFunctions.WriteEVENT_Hit(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 3)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_Toast(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 34)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.WriteEVENT_PetDropSpawn(Value: { { id: number, pos: Vector3, pet: string } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 35)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 18 bytes
	local ARRAY_START_1 = Allocate(18 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.pos
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
		local Length = #Item_1.pet
		-- Allocate 2
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_4, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.pet, Length)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_PetDropRemove(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 36)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_CardDropSpawn(Value: { { id: number, pos: Vector3 } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 37)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 16 bytes
	local ARRAY_START_1 = Allocate(16 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.pos
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_CardDropRemove(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 38)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_HealthSpawn(Value: { { id: number, pos: Vector3 } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 39)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 16 bytes
	local ARRAY_START_1 = Allocate(16 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.pos
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_HealthRemove(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 40)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_TixSpawn(Value: { { id: number, pos: Vector3, amount: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 41)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 20 bytes
	local ARRAY_START_1 = Allocate(20 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.pos
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.Z)
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_4, Item_1.amount)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_TixRemove(Value: { number }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 42)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 4 bytes
	local ARRAY_START_1 = Allocate(4 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_TixCount(Value: number): ()
	-- Allocate BLOCK: 5 bytes
	local BLOCK_START = Allocate(5)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 43)
	buffer.writeu32(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.WriteEVENT_Profile(Value: { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }): ()
	-- Allocate BLOCK: 21 bytes
	local BLOCK_START = Allocate(21)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 44)
	buffer.writeu32(SendBuffer, BLOCK_START + 1, Value.tix)
	buffer.writeu16(SendBuffer, BLOCK_START + 5, Value.bestWave)
	buffer.writeu16(SendBuffer, BLOCK_START + 7, Value.bestLevel)
	buffer.writeu32(SendBuffer, BLOCK_START + 9, Value.bestTime)
	buffer.writeu32(SendBuffer, BLOCK_START + 13, Value.totalKills)
	buffer.writeu32(SendBuffer, BLOCK_START + 17, Value.runs)
end

function SerdesFunctions.WriteEVENT_UfoMove(Value: { { id: number, pos: Vector3 } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 4)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 10 bytes
	local ARRAY_START_1 = Allocate(10 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.pos
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		if Vector.X > 65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0b0_11111_0000000000)
		elseif Vector.X < -65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0b1_11111_0000000000)
		elseif Vector.X ~= Vector.X then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0b1_11111_0000000001)
		elseif Vector.X == 0 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0)
		else
			local float = Vector.X
			local Abosulte = math.abs(float)
			local Interval = math.ldexp(1, math.floor(math.log(Abosulte, 2)) - 10)
			local RoundedValue = (Abosulte // Interval) * Interval
			local Fraction, Exponent = math.frexp(RoundedValue)
			Exponent += 14
			local Mantissa = math.round(if Exponent <= 0
				then Fraction * 0x400 / math.ldexp(1, math.abs(Exponent))
				else Fraction * 0x800) % 0x400
			local Result = Mantissa
				+ math.max(Exponent, 0) * 0x400
				+ if float < 0 then 0x8000 else 0
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Result)
		end
		-- Allocate 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		if Vector.Y > 65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0b0_11111_0000000000)
		elseif Vector.Y < -65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0b1_11111_0000000000)
		elseif Vector.Y ~= Vector.Y then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0b1_11111_0000000001)
		elseif Vector.Y == 0 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0)
		else
			local float = Vector.Y
			local Abosulte = math.abs(float)
			local Interval = math.ldexp(1, math.floor(math.log(Abosulte, 2)) - 10)
			local RoundedValue = (Abosulte // Interval) * Interval
			local Fraction, Exponent = math.frexp(RoundedValue)
			Exponent += 14
			local Mantissa = math.round(if Exponent <= 0
				then Fraction * 0x400 / math.ldexp(1, math.abs(Exponent))
				else Fraction * 0x800) % 0x400
			local Result = Mantissa
				+ math.max(Exponent, 0) * 0x400
				+ if float < 0 then 0x8000 else 0
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, Result)
		end
		-- Allocate 2
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 2
		if Vector.Z > 65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0b0_11111_0000000000)
		elseif Vector.Z < -65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0b1_11111_0000000000)
		elseif Vector.Z ~= Vector.Z then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0b1_11111_0000000001)
		elseif Vector.Z == 0 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0)
		else
			local float = Vector.Z
			local Abosulte = math.abs(float)
			local Interval = math.ldexp(1, math.floor(math.log(Abosulte, 2)) - 10)
			local RoundedValue = (Abosulte // Interval) * Interval
			local Fraction, Exponent = math.frexp(RoundedValue)
			Exponent += 14
			local Mantissa = math.round(if Exponent <= 0
				then Fraction * 0x400 / math.ldexp(1, math.abs(Exponent))
				else Fraction * 0x800) % 0x400
			local Result = Mantissa
				+ math.max(Exponent, 0) * 0x400
				+ if float < 0 then 0x8000 else 0
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, Result)
		end
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UfoFire(Value: { { origin: Vector3, vel: Vector3, t0: number, life: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 45)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 36 bytes
	local ARRAY_START_1 = Allocate(36 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		local Vector = Item_1.origin
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_0, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Z)
		local Vector = Item_1.vel
		-- Allocate 4
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_3, Vector.X)
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Vector.Y)
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Vector.Z)
		-- Allocate 8
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_6, Item_1.t0)
		-- Allocate 4
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_7, Item_1.life)
	end
	-- END ARRAY
end

function SerdesFunctions.WriteEVENT_UfoBeam(Value: { { id: number, target: Vector3, charging: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 5)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 11 bytes
	local ARRAY_START_1 = Allocate(11 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writeu32(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		local Vector = Item_1.target
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		if Vector.X > 65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0b0_11111_0000000000)
		elseif Vector.X < -65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0b1_11111_0000000000)
		elseif Vector.X ~= Vector.X then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0b1_11111_0000000001)
		elseif Vector.X == 0 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, 0)
		else
			local float = Vector.X
			local Abosulte = math.abs(float)
			local Interval = math.ldexp(1, math.floor(math.log(Abosulte, 2)) - 10)
			local RoundedValue = (Abosulte // Interval) * Interval
			local Fraction, Exponent = math.frexp(RoundedValue)
			Exponent += 14
			local Mantissa = math.round(if Exponent <= 0
				then Fraction * 0x400 / math.ldexp(1, math.abs(Exponent))
				else Fraction * 0x800) % 0x400
			local Result = Mantissa
				+ math.max(Exponent, 0) * 0x400
				+ if float < 0 then 0x8000 else 0
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Result)
		end
		-- Allocate 2
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 2
		if Vector.Y > 65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0b0_11111_0000000000)
		elseif Vector.Y < -65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0b1_11111_0000000000)
		elseif Vector.Y ~= Vector.Y then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0b1_11111_0000000001)
		elseif Vector.Y == 0 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, 0)
		else
			local float = Vector.Y
			local Abosulte = math.abs(float)
			local Interval = math.ldexp(1, math.floor(math.log(Abosulte, 2)) - 10)
			local RoundedValue = (Abosulte // Interval) * Interval
			local Fraction, Exponent = math.frexp(RoundedValue)
			Exponent += 14
			local Mantissa = math.round(if Exponent <= 0
				then Fraction * 0x400 / math.ldexp(1, math.abs(Exponent))
				else Fraction * 0x800) % 0x400
			local Result = Mantissa
				+ math.max(Exponent, 0) * 0x400
				+ if float < 0 then 0x8000 else 0
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_2, Result)
		end
		-- Allocate 2
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 2
		if Vector.Z > 65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0b0_11111_0000000000)
		elseif Vector.Z < -65504 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0b1_11111_0000000000)
		elseif Vector.Z ~= Vector.Z then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0b1_11111_0000000001)
		elseif Vector.Z == 0 then
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, 0)
		else
			local float = Vector.Z
			local Abosulte = math.abs(float)
			local Interval = math.ldexp(1, math.floor(math.log(Abosulte, 2)) - 10)
			local RoundedValue = (Abosulte // Interval) * Interval
			local Fraction, Exponent = math.frexp(RoundedValue)
			Exponent += 14
			local Mantissa = math.round(if Exponent <= 0
				then Fraction * 0x400 / math.ldexp(1, math.abs(Exponent))
				else Fraction * 0x800) % 0x400
			local Result = Mantissa
				+ math.max(Exponent, 0) * 0x400
				+ if float < 0 then 0x8000 else 0
			buffer.writeu16(SendBuffer, OPERATION_OFFSET_3, Result)
		end
		-- Allocate 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_4, Item_1.charging)
	end
	-- END ARRAY
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	PlayerAction = {
		On = NOOP
	},
	UpdateTransformDelta = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UpdateTransformFull = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Spawn = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UpdateMobState = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UpdateMobPath = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UpdateMobChase = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	StatSheet = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	MobHealth = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Death = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	FireBubble = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	RemoveBubble = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	SpawnExp = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	RemoveExp = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	LevelUp = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	ChooseCard = {
		On = NOOP
	},
	Aim = {
		On = NOOP
	},
	UseSkill = {
		On = NOOP
	},
	GameOver = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Restart = {
		On = NOOP
	},
	SwitchPlace = {
		On = NOOP
	},
	RoundOver = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Weapons = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Unlocked = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	SwitchWeapons = {
		On = NOOP
	},
	SetMain = {
		On = NOOP
	},
	BuyPet = {
		On = NOOP
	},
	Items = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	MainWeapon = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Effect = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	SummonSpawn = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	SummonRemove = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	HazardSpawn = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	HazardRemove = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UltBeam = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	WaveStart = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Stats = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Hit = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Toast = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	PetDropSpawn = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	PetDropRemove = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	CardDropSpawn = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	CardDropRemove = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	HealthSpawn = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	HealthRemove = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	TixSpawn = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	TixRemove = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	TixCount = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	Profile = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UfoMove = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UfoFire = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	UfoBeam = {
		Fire = NOOP,
		FireAll = NOOP,
		FireList = NOOP,
		FireExcept = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsServer() then
    error("Server network module can only be required from the server.")
end

local Reliable: RemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
if not Reliable then
    local RemoteEvent = Instance.new("RemoteEvent")
    RemoteEvent.Name = BASE_EVENT_NAME .. "_RELIABLE_REMOTE"
    RemoteEvent.Parent = ReplicatedStorage
    Reliable = RemoteEvent
end

local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent
if not Unreliable then
    local UnreliableRemoteEvent = Instance.new("UnreliableRemoteEvent")
    UnreliableRemoteEvent.Name = BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE"
    UnreliableRemoteEvent.Parent = ReplicatedStorage
    Unreliable = UnreliableRemoteEvent
end

local PlayersMap: {[Player]: BufferSave} = {}

Players.PlayerRemoving:Connect(function(Player)
    PlayersMap[Player] = nil
end)

local function StepReplication()
    for Player, Send in PlayersMap do
        if Send.Cursor <= 0 then
            continue
        end

        local Buffer = buffer.create(Send.Cursor)
        buffer.copy(Buffer, 0, Send.Buffer, 0, Send.Cursor)
        Reliable:FireClient(Player, Buffer, Send.Instances)

        Send.Size = 64
        Send.Cursor = 0
        Send.Buffer = buffer.create(64)
        table.clear(Send.Instances)
    end
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: number = SerdesFunctions.ReadEVENT_PlayerAction()
			if Events.Reliable[0] ~= nil then
				SerdesEvent = "PlayerAction"
				Events.Reliable[0](Player, Value)
			else
				if #Queue.Reliable[0] > 256 then
					warn("[Blink]: Event queue of \"PlayerAction\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[0], {Player, Value} :: {any})
			end
		elseif Index == 13 then
			local Value: number = SerdesFunctions.ReadEVENT_ChooseCard()
			if Events.Reliable[13] ~= nil then
				SerdesEvent = "ChooseCard"
				Events.Reliable[13](Player, Value)
			else
				if #Queue.Reliable[13] > 256 then
					warn("[Blink]: Event queue of \"ChooseCard\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[13], {Player, Value} :: {any})
			end
		elseif Index == 14 then
			local Value: number = SerdesFunctions.ReadEVENT_UseSkill()
			if Events.Reliable[14] ~= nil then
				SerdesEvent = "UseSkill"
				Events.Reliable[14](Player, Value)
			else
				if #Queue.Reliable[14] > 256 then
					warn("[Blink]: Event queue of \"UseSkill\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[14], {Player, Value} :: {any})
			end
		elseif Index == 16 then
			local Value: number = SerdesFunctions.ReadEVENT_Restart()
			if Events.Reliable[16] ~= nil then
				SerdesEvent = "Restart"
				Events.Reliable[16](Player, Value)
			else
				if #Queue.Reliable[16] > 256 then
					warn("[Blink]: Event queue of \"Restart\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[16], {Player, Value} :: {any})
			end
		elseif Index == 17 then
			local Value: number = SerdesFunctions.ReadEVENT_SwitchPlace()
			if Events.Reliable[17] ~= nil then
				SerdesEvent = "SwitchPlace"
				Events.Reliable[17](Player, Value)
			else
				if #Queue.Reliable[17] > 256 then
					warn("[Blink]: Event queue of \"SwitchPlace\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[17], {Player, Value} :: {any})
			end
		elseif Index == 21 then
			local Value: { string } = SerdesFunctions.ReadEVENT_SwitchWeapons()
			if Events.Reliable[21] ~= nil then
				SerdesEvent = "SwitchWeapons"
				Events.Reliable[21](Player, Value)
			else
				if #Queue.Reliable[21] > 256 then
					warn("[Blink]: Event queue of \"SwitchWeapons\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[21], {Player, Value} :: {any})
			end
		elseif Index == 22 then
			local Value: string = SerdesFunctions.ReadEVENT_SetMain()
			if Events.Reliable[22] ~= nil then
				SerdesEvent = "SetMain"
				Events.Reliable[22](Player, Value)
			else
				if #Queue.Reliable[22] > 256 then
					warn("[Blink]: Event queue of \"SetMain\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[22], {Player, Value} :: {any})
			end
		elseif Index == 23 then
			local Value: string = SerdesFunctions.ReadEVENT_BuyPet()
			if Events.Reliable[23] ~= nil then
				SerdesEvent = "BuyPet"
				Events.Reliable[23](Player, Value)
			else
				if #Queue.Reliable[23] > 256 then
					warn("[Blink]: Event queue of \"BuyPet\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[23], {Player, Value} :: {any})
			end
		end
	end
end)
Unreliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 2 then
			local Value: Vector3 = SerdesFunctions.ReadEVENT_Aim()
			if Events.Unreliable[2] ~= nil then
				SerdesEvent = "Aim"
				Events.Unreliable[2](Player, Value)
			end
		end
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	PlayerAction = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[0] = Listener
			for Index, Arguments in Queue.Reliable[0] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[0] = {}
			return function (): ()
				if Events.Reliable[0] == Listener then
					Events.Reliable[0] = nil
				end
			end
		end
	},
	UpdateTransformDelta = {
		Fire = function(Player: Player, Value: { base: number, deltas: { number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateTransformDelta(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { base: number, deltas: { number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateTransformDelta(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { base: number, deltas: { number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateTransformDelta(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { base: number, deltas: { number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateTransformDelta(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
	UpdateTransformFull = {
		Fire = function(Player: Player, Value: { { id: number, dp: Vector3 } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_UpdateTransformFull(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, dp: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateTransformFull(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, dp: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateTransformFull(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, dp: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateTransformFull(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Spawn = {
		Fire = function(Player: Player, Value: { { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Spawn(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Spawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Spawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, slot: number, pos: Vector3, size: number, type: string, tier: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Spawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	UpdateMobState = {
		Fire = function(Player: Player, Value: { { id: number, state: {[string]: string} } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_UpdateMobState(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, state: {[string]: string} } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobState(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, state: {[string]: string} } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobState(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, state: {[string]: string} } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobState(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	UpdateMobPath = {
		Fire = function(Player: Player, Value: { { id: number, from: number, to: number, speed: number, startTime: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_UpdateMobPath(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, from: number, to: number, speed: number, startTime: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobPath(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, from: number, to: number, speed: number, startTime: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobPath(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, from: number, to: number, speed: number, startTime: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobPath(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	UpdateMobChase = {
		Fire = function(Player: Player, Value: { { id: number, target: Instance, speed: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_UpdateMobChase(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, target: Instance, speed: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobChase(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, target: Instance, speed: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobChase(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, target: Instance, speed: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UpdateMobChase(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	StatSheet = {
		Fire = function(Player: Player, Value: { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_StatSheet(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_StatSheet(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_StatSheet(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { maxHp: number, hpRegen: number, atk: number, walkSpeed: number, crit: number, critDmg: number, pickup: number, haste: number, expMult: number, tixRate: number, atkSize: number, knock: number, skillCd: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_StatSheet(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	MobHealth = {
		Fire = function(Player: Player, Value: { { id: number, hp: number, max: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_MobHealth(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, hp: number, max: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_MobHealth(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, hp: number, max: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_MobHealth(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, hp: number, max: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_MobHealth(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Death = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Death(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Death(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Death(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Death(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	FireBubble = {
		Fire = function(Player: Player, Value: { { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_FireBubble(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } }): ()
			Load()
			SerdesFunctions.WriteEVENT_FireBubble(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } }): ()
			Load()
			SerdesFunctions.WriteEVENT_FireBubble(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, origin: Vector3, velocity: Vector3, t0: number, life: number, color: Vector3, size: number, shape: number? } }): ()
			Load()
			SerdesFunctions.WriteEVENT_FireBubble(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	RemoveBubble = {
		Fire = function(Player: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_RemoveBubble(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_RemoveBubble(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_RemoveBubble(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_RemoveBubble(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
	SpawnExp = {
		Fire = function(Player: Player, Value: { { id: number, pos: Vector3, tier: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_SpawnExp(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, pos: Vector3, tier: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SpawnExp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, pos: Vector3, tier: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SpawnExp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, pos: Vector3, tier: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SpawnExp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	RemoveExp = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_RemoveExp(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_RemoveExp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_RemoveExp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_RemoveExp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	LevelUp = {
		Fire = function(Player: Player, Value: { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_LevelUp(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }): ()
			Load()
			SerdesFunctions.WriteEVENT_LevelUp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }): ()
			Load()
			SerdesFunctions.WriteEVENT_LevelUp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { source: number, cards: { { id: number, title: string, desc: string, kind: number, weapon: string } } }): ()
			Load()
			SerdesFunctions.WriteEVENT_LevelUp(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	ChooseCard = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[13] = Listener
			for Index, Arguments in Queue.Reliable[13] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[13] = {}
			return function (): ()
				if Events.Reliable[13] == Listener then
					Events.Reliable[13] = nil
				end
			end
		end
	},
	Aim = {
		On = function(Listener: (Player: Player, Value: Vector3) -> ()): () -> ()
			Events.Unreliable[2] = Listener
			return function (): ()
				if Events.Unreliable[2] == Listener then
					Events.Unreliable[2] = nil
				end
			end
		end
	},
	UseSkill = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[14] = Listener
			for Index, Arguments in Queue.Reliable[14] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[14] = {}
			return function (): ()
				if Events.Reliable[14] == Listener then
					Events.Reliable[14] = nil
				end
			end
		end
	},
	GameOver = {
		Fire = function(Player: Player, Value: { time: number, level: number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_GameOver(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { time: number, level: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_GameOver(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { time: number, level: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_GameOver(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { time: number, level: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_GameOver(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Restart = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[16] = Listener
			for Index, Arguments in Queue.Reliable[16] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[16] = {}
			return function (): ()
				if Events.Reliable[16] == Listener then
					Events.Reliable[16] = nil
				end
			end
		end
	},
	SwitchPlace = {
		On = function(Listener: (Player: Player, Value: number) -> ()): () -> ()
			Events.Reliable[17] = Listener
			for Index, Arguments in Queue.Reliable[17] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[17] = {}
			return function (): ()
				if Events.Reliable[17] == Listener then
					Events.Reliable[17] = nil
				end
			end
		end
	},
	RoundOver = {
		Fire = function(Player: Player, Value: number): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_RoundOver(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_RoundOver(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_RoundOver(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_RoundOver(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Weapons = {
		Fire = function(Player: Player, Value: { { id: string, level: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Weapons(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: string, level: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Weapons(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: string, level: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Weapons(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: string, level: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Weapons(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Unlocked = {
		Fire = function(Player: Player, Value: { string }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Unlocked(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { string }): ()
			Load()
			SerdesFunctions.WriteEVENT_Unlocked(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { string }): ()
			Load()
			SerdesFunctions.WriteEVENT_Unlocked(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { string }): ()
			Load()
			SerdesFunctions.WriteEVENT_Unlocked(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	SwitchWeapons = {
		On = function(Listener: (Player: Player, Value: { string }) -> ()): () -> ()
			Events.Reliable[21] = Listener
			for Index, Arguments in Queue.Reliable[21] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[21] = {}
			return function (): ()
				if Events.Reliable[21] == Listener then
					Events.Reliable[21] = nil
				end
			end
		end
	},
	SetMain = {
		On = function(Listener: (Player: Player, Value: string) -> ()): () -> ()
			Events.Reliable[22] = Listener
			for Index, Arguments in Queue.Reliable[22] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[22] = {}
			return function (): ()
				if Events.Reliable[22] == Listener then
					Events.Reliable[22] = nil
				end
			end
		end
	},
	BuyPet = {
		On = function(Listener: (Player: Player, Value: string) -> ()): () -> ()
			Events.Reliable[23] = Listener
			for Index, Arguments in Queue.Reliable[23] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[23] = {}
			return function (): ()
				if Events.Reliable[23] == Listener then
					Events.Reliable[23] = nil
				end
			end
		end
	},
	Items = {
		Fire = function(Player: Player, Value: { { id: string, level: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Items(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: string, level: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Items(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: string, level: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Items(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: string, level: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Items(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	MainWeapon = {
		Fire = function(Player: Player, Value: number): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_MainWeapon(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_MainWeapon(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_MainWeapon(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_MainWeapon(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Effect = {
		Fire = function(Player: Player, Value: { { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Effect(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Effect(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Effect(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { kind: number, pos: Vector3, radius: number, dir: Vector3, arc: number?, color: Vector3? } }): ()
			Load()
			SerdesFunctions.WriteEVENT_Effect(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	SummonSpawn = {
		Fire = function(Player: Player, Value: { { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_SummonSpawn(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SummonSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SummonSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, kind: number, origin: Vector3, velocity: Vector3, t0: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_SummonSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	SummonRemove = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_SummonRemove(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_SummonRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_SummonRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_SummonRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	HazardSpawn = {
		Fire = function(Player: Player, Value: { { id: number, kind: number, pos: Vector3, radius: number, life: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_HazardSpawn(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, kind: number, pos: Vector3, radius: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_HazardSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, kind: number, pos: Vector3, radius: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_HazardSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, kind: number, pos: Vector3, radius: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_HazardSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	HazardRemove = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_HazardRemove(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_HazardRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_HazardRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_HazardRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	UltBeam = {
		Fire = function(Player: Player, Value: { active: number, life: number, width: number, length: number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_UltBeam(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { active: number, life: number, width: number, length: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_UltBeam(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { active: number, life: number, width: number, length: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_UltBeam(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { active: number, life: number, width: number, length: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_UltBeam(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	WaveStart = {
		Fire = function(Player: Player, Value: number): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_WaveStart(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_WaveStart(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_WaveStart(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_WaveStart(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Stats = {
		Fire = function(Player: Player, Value: { level: number, xp: number, xpNext: number, hp: number, maxHp: number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Stats(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { level: number, xp: number, xpNext: number, hp: number, maxHp: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Stats(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { level: number, xp: number, xpNext: number, hp: number, maxHp: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Stats(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { level: number, xp: number, xpNext: number, hp: number, maxHp: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Stats(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Hit = {
		Fire = function(Player: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Hit(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Hit(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Hit(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Hit(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
	Toast = {
		Fire = function(Player: Player, Value: string): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Toast(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: string): ()
			Load()
			SerdesFunctions.WriteEVENT_Toast(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: string): ()
			Load()
			SerdesFunctions.WriteEVENT_Toast(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: string): ()
			Load()
			SerdesFunctions.WriteEVENT_Toast(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	PetDropSpawn = {
		Fire = function(Player: Player, Value: { { id: number, pos: Vector3, pet: string } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_PetDropSpawn(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, pos: Vector3, pet: string } }): ()
			Load()
			SerdesFunctions.WriteEVENT_PetDropSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, pos: Vector3, pet: string } }): ()
			Load()
			SerdesFunctions.WriteEVENT_PetDropSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, pos: Vector3, pet: string } }): ()
			Load()
			SerdesFunctions.WriteEVENT_PetDropSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	PetDropRemove = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_PetDropRemove(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_PetDropRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_PetDropRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_PetDropRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	CardDropSpawn = {
		Fire = function(Player: Player, Value: { { id: number, pos: Vector3 } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_CardDropSpawn(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_CardDropSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_CardDropSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_CardDropSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	CardDropRemove = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_CardDropRemove(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_CardDropRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_CardDropRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_CardDropRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	HealthSpawn = {
		Fire = function(Player: Player, Value: { { id: number, pos: Vector3 } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_HealthSpawn(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_HealthSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_HealthSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_HealthSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	HealthRemove = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_HealthRemove(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_HealthRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_HealthRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_HealthRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	TixSpawn = {
		Fire = function(Player: Player, Value: { { id: number, pos: Vector3, amount: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_TixSpawn(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { id: number, pos: Vector3, amount: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_TixSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { id: number, pos: Vector3, amount: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_TixSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, pos: Vector3, amount: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_TixSpawn(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	TixRemove = {
		Fire = function(Player: Player, Value: { number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_TixRemove(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_TixRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_TixRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { number }): ()
			Load()
			SerdesFunctions.WriteEVENT_TixRemove(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	TixCount = {
		Fire = function(Player: Player, Value: number): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_TixCount(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_TixCount(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_TixCount(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: number): ()
			Load()
			SerdesFunctions.WriteEVENT_TixCount(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	Profile = {
		Fire = function(Player: Player, Value: { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_Profile(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Profile(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Profile(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { tix: number, bestWave: number, bestLevel: number, bestTime: number, totalKills: number, runs: number }): ()
			Load()
			SerdesFunctions.WriteEVENT_Profile(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	UfoMove = {
		Fire = function(Player: Player, Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoMove(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoMove(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoMove(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, pos: Vector3 } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoMove(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
	UfoFire = {
		Fire = function(Player: Player, Value: { { origin: Vector3, vel: Vector3, t0: number, life: number } }): ()
			Load(PlayersMap[Player])
			SerdesFunctions.WriteEVENT_UfoFire(Value)
			PlayersMap[Player] = Save()
		end,
		FireAll = function(Value: { { origin: Vector3, vel: Vector3, t0: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoFire(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireList = function(List: {Player}, Value: { { origin: Vector3, vel: Vector3, t0: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoFire(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in List do
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
		FireExcept = function(Except: Player, Value: { { origin: Vector3, vel: Vector3, t0: number, life: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoFire(Value)
			local Buffer, Size, Instances = SendBuffer, SendCursor, SendInstances
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Load(PlayersMap[Player])
				local Position = Allocate(Size)
				buffer.copy(SendBuffer, Position, Buffer, 0, Size)
				table.move(Instances, 1, #Instances, #SendInstances + 1, SendInstances)
				PlayersMap[Player] = Save()
			end
		end,
	},
	UfoBeam = {
		Fire = function(Player: Player, Value: { { id: number, target: Vector3, charging: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoBeam(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireClient(Player, Buffer, SendInstances)
		end,
		FireAll = function(Value: { { id: number, target: Vector3, charging: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoBeam(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireAllClients(Buffer, SendInstances)
		end,
		FireList = function(List: {Player}, Value: { { id: number, target: Vector3, charging: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoBeam(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in List do
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
		FireExcept = function(Except: Player, Value: { { id: number, target: Vector3, charging: number } }): ()
			Load()
			SerdesFunctions.WriteEVENT_UfoBeam(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			for _, Player in Players:GetPlayers() do
				if Player == Except then
					continue
				end
				Unreliable:FireClient(Player, Buffer, SendInstances)
			end
		end,
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL